import Icon from "../../components/Icon";

export default function BackpackCardLocked() {
  return (
    <span className="backpack-card-locked">
      <Icon name="lock" size={12} />
      Locked
    </span>
  );
}
